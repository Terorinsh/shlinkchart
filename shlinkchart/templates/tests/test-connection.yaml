apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shlinkchart.fullname" . }}-test-connection"
  labels:
    {{- include "shlinkchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shlinkchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
